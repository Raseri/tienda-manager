# ===================================
# TIENDA MANAGER - Variables de entorno
# Copia este archivo como .env y llena los valores
# NUNCA subas .env a git
# ===================================

# MySQL (en producción usa Aiven, PlanetScale, o Railway)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=TU_PASSWORD_AQUI
DB_NAME=tienda_manager

# Servidor
PORT=3000

# JWT - USA UN SECRETO LARGO Y ALEATORIO EN PRODUCCION
# Genera uno con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CAMBIA_ESTO_POR_UN_SECRETO_SEGURO_EN_PRODUCCION
JWT_EXPIRES_IN=7d
